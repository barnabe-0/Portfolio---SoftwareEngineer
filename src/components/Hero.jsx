import React, { Fragment } from 'react';

const Hero = () => {
    return (
        <Fragment>
            <section id="home" className="relative min-h-screen flex items-center justify-center pt-16 pb-20">
                <div className="absolute inset-0 bg-[url('./assets/Image_fundo.png')] bg-center bg-cover bg-no-repeat ">

                </div>

            </section>
        </Fragment>
    );
};

export default Hero;